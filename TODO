Post Operations
  √ Remove Post 'changed' logic
  - Return modified posts instead of just #?
  - Expose all modifications in one post operation and trigger with flags?

Post
  √ Move 'changed' logic inside of Post
  Generate tags
    √ Handle HTML comments

Reporter class?
  Add preview report of
    - # Untagged items (sim mode)
    - Resulting comment and tags (sim mode)
  Add live report
    - More detailed action summary (live mode)

Progress indicator
  - Show # actually being processed (currently shows all available for dk md)
